# LivePoll Environment Configuration

# Public hostname for QR code URLs (without protocol)
HOSTNAME=localhost

# Server port
PORT=8000

# Secret key for session tokens (change in production!)
SECRET_KEY=change-me-in-production

# Database connection string
DATABASE_URL=sqlite+aiosqlite:///./livepoll.db
